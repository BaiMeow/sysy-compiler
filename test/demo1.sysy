const float abc = 1+1;

int abcd(int a,int b){
    while(a<10)
        if (a%2==1)
            a=a+1;
        else {
            a=a+2;
        }

    return a+b;
}

int main(){
    float abc=1.1;
    int arr[10] = {1,2,3};
    arr[3] = -abcd(1,1);
    if (1==1) return 1;
}

